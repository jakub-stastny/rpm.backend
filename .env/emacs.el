; Don't forget to see README.org in this directory
; in order to install required packages using literate DevOps.

(add-to-list 'load-path (expand-file-name "~/.zsh/environments/elisp"))

(load "literate-programming")
(load-theme 'adwaita)
